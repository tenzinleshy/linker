{% extends 'base.html.twig' %}

{% block body %}
<div class="jumbotron text-center">
    <h1>Share this link</h1>
</div>
<div class="container">
    <div class="row">
        <div class="col-sm-15">

                    <div class="panel panel-default">
                        <div class="panel-heading">Short URL</div>
                        <div class="panel-body"><a href="{{ url('homepage') }}{{ link.shortUrlId }}" target="_blank">{{ url('homepage') }}{{ link.shortUrlId }}</a></div>
                    </div>
                </td>
    <ul>
        <li>
            <a href="{{ path('link_index') }}">Show link list</a>
        </li>
        {#<li>#}
            {#<a href="{{ path('link_edit', { 'id': link.id }) }}">Edit</a>#}
        {#</li>#}
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}
        </li>
    </ul>
        </div>
    </div>
</div>
{% endblock %}
